const COMMERCIAL_INVITATION_STATUS = Object.freeze({
    PENDING: 'pending',
    ACCEPTED: 'accepted',
    EXPIRED: 'expired',
    REVOKED: 'revoked',
});

const COMMERCIAL_INVITATION_DELIVERY_STATUS = Object.freeze({
    PENDING: 'pending',
    SENT: 'sent',
    FAILED: 'failed',
});

const COMMERCIAL_INVITATION_TOKEN_BYTES = 32;
const COMMERCIAL_INVITATION_TTL_DAYS = 7;

export {
    COMMERCIAL_INVITATION_DELIVERY_STATUS,
    COMMERCIAL_INVITATION_STATUS,
    COMMERCIAL_INVITATION_TOKEN_BYTES,
    COMMERCIAL_INVITATION_TTL_DAYS,
};
